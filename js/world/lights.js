import * as THREE from 'three';

// A criação de luzes agora é gerenciada pelo DayNightCycle.
// Mantemos este arquivo para futuras luzes estáticas (ex: postes, tochas).
export function createLights(scene) {
    // Não faz nada por enquanto.
}